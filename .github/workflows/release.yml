name: Version and Release

# The whole release process, from a merged pull request to a downloadable
# module. Nothing here is started by hand.
#
#   1. A pull request declares its bump as a `.changeset/*.md` file.
#   2. Merging it to `main` runs this workflow, which opens or updates the
#      **Version Packages** pull request: the version bump plus the rewritten
#      CHANGELOG.md. An unreleased state is therefore a pull request sitting in
#      the queue, not an absence nobody can see.
#   3. Merging *that* runs this workflow again with no changesets left, so it
#      builds the module, packages it, and cuts the `v<version>` tag and the
#      GitHub Release that Foundry installs from.
#
# This repository ships a **module**, not an npm package, so there is no
# `publish:` step and no registry credential. The release *is* the tag and its
# two assets: `module.zip`, and the `module.json` an already-installed module
# checks for updates against.

on:
    push:
        branches: [main]
    # Manual recovery: re-runs the decision for a version that is merged and
    # versioned but never got its release. Cutting an existing tag is skipped
    # rather than duplicated, so running this is safe.
    workflow_dispatch:

# contents: write      -> push the version branch, create the tag and Release
# pull-requests: write -> open and update the Version Packages pull request
permissions:
    contents: write
    pull-requests: write

# Two pushes to main must never race on the version pull request or the tag.
concurrency:
    group: release
    cancel-in-progress: false

jobs:
    release:
        name: Version and release sohl-thalorna
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v7
              with:
                  # `changeset version` reads the history to build the changelog, and
                  # the decision below asks the remote for existing tags.
                  fetch-depth: 0

            - name: Setup Node
              uses: actions/setup-node@v7
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            # With changesets pending: opens or updates the Version Packages pull
            # request and stops. With none pending — the state right after that pull
            # request merges — `hasChangesets` is false, which is the signal that a
            # freshly-versioned commit just landed.
            - name: Create or update the Version Packages PR
              id: changesets
              uses: changesets/action@v2
              with:
                  # The repository's own script, not a second spelling of it. It must
                  # stay ONE command: this input is tokenized and exec'd, never handed
                  # to a shell, so a `&&` chain written here would be read as arguments
                  # to the first command. That is exactly what broke every release in
                  # `@heroiclands/content-build` (content-build#74) — an npm script is
                  # the seam that does get a shell.
                  version-script: npm run changeset:version
                  commit-message: "chore(release): version packages"
                  pr-title: "chore(release): version packages"
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            # Release only when the version pull request has merged: no changesets
            # remain AND this version has not already been tagged. Both halves are
            # needed — the first says a bump just landed, the second keeps an
            # ordinary push (or a re-run) from cutting the same release twice.
            - name: Decide whether to release
              id: decide
              if: steps.changesets.outputs.hasChangesets == 'false'
              run: |
                  VERSION=$(node -p "require('./package.json').version")
                  TAG="v$VERSION"
                  if git ls-remote --exit-code --tags origin "refs/tags/$TAG" >/dev/null 2>&1; then
                      echo "Tag $TAG already exists — nothing to release."
                      echo "release=false" >> "$GITHUB_OUTPUT"
                  else
                      echo "Version $VERSION is untagged — releasing $TAG."
                      echo "release=true" >> "$GITHUB_OUTPUT"
                      echo "tag=$TAG" >> "$GITHUB_OUTPUT"
                  fi

            # `build:noci` rather than `build`, which begins with its own `npm ci` —
            # the dependencies are already installed above. It runs the format check,
            # the content lint and the pack compile, so a release cannot be cut from
            # a tree that does not build or does not lint.
            - name: Build module
              if: steps.decide.outputs.release == 'true'
              run: npm run build:noci

            - name: Package module
              if: steps.decide.outputs.release == 'true'
              run: npm run build:pack-release

            # Written to a file rather than a step output: a version's section can be
            # very large when many changesets have accumulated between releases, and
            # passing that much text through an action input delivers it via the
            # process environment, which the runner will refuse to spawn past a
            # certain size. `body_path` reads the file directly.
            - name: Read changelog section for this version
              id: changelog
              if: steps.decide.outputs.release == 'true'
              run: |
                  # Everything between the first "## " heading and the next one,
                  # excluding the version header itself — the release title already
                  # shows it.
                  awk '/^## /{c++; if (c>=2) exit; next} c==1{print}' CHANGELOG.md > build/release-notes.md

            # Creates the v<version> tag and the GitHub Release, attaching the two
            # assets Foundry installs and updates from.
            - name: Create GitHub Release
              if: steps.decide.outputs.release == 'true'
              uses: softprops/action-gh-release@v3
              with:
                  tag_name: ${{ steps.decide.outputs.tag }}
                  name: Release ${{ steps.decide.outputs.tag }}
                  body_path: build/release-notes.md
                  files: |
                      build/dist/module.zip
                      build/dist/module.json
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
